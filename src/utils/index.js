import { reactive } from "vue";

export const dataSearch = reactive({
  data: [],
});

export const movieHome = reactive({
  data: [],
  avengers: [],
  callOfDuty: [],
  transformers: [],
  vampire: [],
});

export const showDetailMovie = reactive({
  data: [],
});
